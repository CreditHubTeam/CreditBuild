// recordPointEvent(userId, pointsDelta, category, reason, source, challengeId, fanClubId, multiplier, txHash, mocaTokenEquivalent): Ghi lại sự kiện điểm.
// getTotalPoints(userId): Lấy tổng điểm của người dùng.
// getPointsByCategory(userId, category): Lấy điểm theo danh mục của người dùng.